#include "holberton.h"
/**
 * _strlen - print the lenght of the word
 * @s: Each of the characters that will come int
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int position;

	while (position[s] != '\0')
	{
		position++;
	}
	return (position);
}
