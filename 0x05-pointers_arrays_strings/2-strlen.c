#include "holberton.h"
/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int position;

	while (position[s] != '\0')
	{
		position++;
	}
	return (position);
}
